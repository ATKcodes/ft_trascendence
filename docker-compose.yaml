version: '3'
services:
  frontend:
    build: ./front_end/
    ports:
      - "8080:80"
  db:
    build: ./db/
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: postre
      POSTGRES_PASSWORD: mypassword
  backend:
    depends_on:
      - db
    build: ./backend/
    ports:
      - "9000:9000"
    environment:
      DJANGO_SETTINGS_MODULE: trascendence.settings